<div class="pageTitle">
  <h1>Statuses</h1>
</div>

<div class="toolbar">
  <div class="leftItem regularButton">
    <a href="<%= Rho::RhoConfig.start_path %>">Home</a>
  </div>
  <div class="rightItem regularButton">
    <a class="button" href="<%= url_for :action => :new %>">New</a>
  </div>
</div>

<div class="content">
  <ul>
    <% @statuses.each do |status| %>
    
        <li>
          <a href="<%= url_for :action => :show, :id => status.object %>">
            <span class="title"><%= status.init_data %></span><span class="disclosure_indicator"></span>
          </a>
        </li>
    
    <% end %>
  </ul>
</div>