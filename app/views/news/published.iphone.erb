<%@news.each do |news|%>

	<div id="news_<%=news.id%>" class="news">
		<h3><%= tt(news,'title') %></a></h3>
		<span><%=news.news_date.strftime("%Y %m %d")%></span>
		<%= tt(news,'body') %>
	</div>
	
<%end%>

<%=will_paginate @news%>